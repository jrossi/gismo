// A Rust file with syntax errors

fn main() {
    println!("This has a syntax error"
    // Missing closing parenthesis and semicolon
    
    let x = 5
    // Missing semicolon
    
    if x > 3 // Missing opening brace
        println!("x is greater than 3");
    }
    
// Missing closing brace for main function

struct BrokenStruct {
    field1: String,
    field2  // Missing type annotation
}